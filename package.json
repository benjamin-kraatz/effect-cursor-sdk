{
  "name": "effect-cursor-sdk",
  "version": "0.1.1",
  "description": "Effect-based wrapper around the Cursor SDK",
  "keywords": [
    "cursor",
    "effect",
    "sdk"
  ],
  "homepage": "https://github.com/benjamin-kraatz/effect-cursor-sdk#readme",
  "bugs": {
    "url": "https://github.com/benjamin-kraatz/effect-cursor-sdk/issues"
  },
  "license": "MIT",
  "author": "",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/benjamin-kraatz/effect-cursor-sdk.git"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "type": "module",
  "sideEffects": false,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsdown",
    "changeset": "changeset",
    "prepare": "effect-language-service patch",
    "prepack": "bun run build",
    "prepublishOnly": "bun run verify:publish",
    "release": "bun run verify:publish && changeset publish",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "oxlint .",
    "lint:fix": "oxlint --fix .",
    "format": "oxfmt .",
    "format:check": "oxfmt --check .",
    "lint:package": "publint",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "version": "changeset version",
    "verify:publish": "bun run typecheck && bun run lint && bun run test && bun run build && bun run lint:package"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@cursor/sdk": "^1.0.9"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.6.0",
    "@changesets/cli": "^2.31.0",
    "@effect/language-service": "^0.85.1",
    "@effect/vitest": "4.0.0-beta.57",
    "@types/bun": "latest",
    "@vitest/coverage-v8": "^4.1.5",
    "effect": "^4.0.0-beta.57",
    "oxfmt": "^0.46.0",
    "oxlint": "^1.61.0",
    "publint": "^0.3.12",
    "tsdown": "^0.21.10",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5"
  },
  "peerDependencies": {
    "effect": "^4.0.0-beta.57",
    "typescript": "^6.0.3"
  },
  "peerDependenciesMeta": {
    "typescript": {
      "optional": true
    }
  }
}
