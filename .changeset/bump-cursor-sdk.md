---
"effect-cursor-sdk": patch
---

Bump `@cursor/sdk` to `^1.0.10`, bump `oxfmt` to `^0.47.0`, and refresh `bun.lock`. Add `effect-cursor-sdk` as a workspace self-dependency so Bun resolves the package consistently.
