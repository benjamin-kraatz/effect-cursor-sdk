---
"effect-cursor-sdk": patch
---

Document the PR changeset agent workflow, fix the workflow commit guard to detect new untracked changeset files, and refresh README examples (streaming snippets, Effect integration).
