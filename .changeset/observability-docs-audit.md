---
"effect-cursor-sdk": minor
---

Add `cursor-observability` helpers (stream metrics, catalog retry/timeout defaults, safe summaries). Extend mocks and run wiring; warn when `CURSOR_API_KEY` is missing during config load. Add `scripts/sdk-surface-audit.ts`, docs (`RECIPES`, `RELEASE_CHECKLIST`, `SDK_COVERAGE`, `MIGRATION_NEXT_MAJOR`), and small CI / example updates.
