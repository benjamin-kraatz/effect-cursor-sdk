---
"effect-cursor-sdk": patch
---

Bump `@cursor/sdk` to 1.0.13 and map the SDK's `AgentBusyError` to a new `CursorAgentBusyError` tagged error, re-exported through service interfaces and `cursor-types`.
