---
"effect-cursor-sdk": minor
---

Bump `@cursor/sdk` to 1.0.26 and Effect to 4.0.0-beta.103. Expose `getUsage` on `CursorAgentService` / `CursorInspectionService` (and mocks), and re-export the related usage types.
