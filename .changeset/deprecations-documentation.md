---
"effect-cursor-sdk": patch
---

Add root `DEPRECATIONS.md` as the canonical deprecation and next-major migration guide; link from the README and include the file in the published npm package. Extend `CursorAgentService` TSDoc with planned renames (`createFromConfig` → `create`, etc.).
